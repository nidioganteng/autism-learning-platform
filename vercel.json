{
  "version": 2,
  "framework": null,
  "buildCommand": "npm run build && mkdir -p dist && cp -r public/build dist/ && cp -r public/img dist/ && cp public/favicon.ico dist/ 2>/dev/null || true",
  "outputDirectory": "dist",
  "build": {
    "env": {
      "COMPOSER_NO_DEV": "1"
    }
  },
  "functions": {
    "api/index.php": {
      "runtime": "vercel-php@0.7.4"
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/api/index.php" }
  ]
}
